# HR ERP CODEOWNERS
# Owner account SafetyMP is a user (not an org), so team handles like
# @SafetyMP/hr-erp-* are invalid until an organization + teams exist.
# Route reviews to @SafetyMP for now; swap to teams when provisioned.

# Platform / pipelines (first match wins for overlapping paths unless configured otherwise)
/.github/workflows/ @SafetyMP
/.github/CODEOWNERS @SafetyMP

# Payroll & compensation calculations
/packages/payroll-calc/ @SafetyMP

# Regulatory rule packs interpreted by backends
/docs/compliance/ @SafetyMP

# Security-sensitive surfaces — treat Prisma DDL + JWT gate as authoritative
/prisma/ @SafetyMP
/lib/security/ @SafetyMP
/middleware.ts @SafetyMP
/docs/security/ @SafetyMP
/scripts/security-scan.mjs @SafetyMP

# API contracts — breaking changes need explicit review
/contracts/ @SafetyMP
/proto/ @SafetyMP

# Fallback
* @SafetyMP
