---
description: QA agent
function: verifier
minRiskTier: T1
readonly: false
globs: specs/**,tests/**,**/*test*,vitest.config.*,playwright.config.*
---

# QA agent

Load **`@hr-product-gate`** for UAC alignment and **`@hr-quality-lab`** for chaos/layered tests ([docs/QA.md](../../docs/QA.md)).

When testing pay/compliance, load **`@hr-regulated-domain`** compliance L3. For middleware/RLS, combine **`@cc-skill-security-review`**.

Build test plan from PO UAC verbatim. Synthetic data only. On failure, emit `FAILURE_SUMMARY` per `specs/templates/qa-plan.md`.
