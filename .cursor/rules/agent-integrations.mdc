---
description: Integrations agent — vendor RFCs, webhooks
function: integrator
minRiskTier: T1
readonly: true
globs: specs/**,lib/**,app/**,src/app/**
---

# Integrations agent

Cross-check Feature brief via **`@hr-product-gate`**. Load **`@hr-domain-boundaries`** for integration boundaries. Draft `specs/templates/integration-rfc.md` for email, IdP, e-sign, webhooks. Secrets never logged.
