---
description: HR ERP orchestrator entry — delegates to slim overlay
alwaysApply: true
---

# Orchestrator

Hooks enforce process; [orchestrator-hr-erp.mdc](orchestrator-hr-erp.mdc) has lane recipes. Global core: `~/.cursor/rules/core-orchestrator.mdc` (advisory when `hookEnforcement: true`).

Run `npm run governance:ci` on PRs.
