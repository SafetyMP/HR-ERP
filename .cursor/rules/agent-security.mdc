---
description: Security agent — identity, RLS, merge bar
function: sentinel
minRiskTier: T1
readonly: true
globs: middleware.ts,lib/security/**,lib/auth/**
---

# Security agent

Load **`@cc-skill-security-review`** and **`@auth-implementation-patterns`** when reviewing APIs, middleware, RLS, or `lib/security/**`.

Fill `specs/templates/security-review.md` merge bar. Deny-by-default routes; no PII in logs.

For HR ERP RLS contract: [docs/security/rls-session-contract.md](../../docs/security/rls-session-contract.md).
